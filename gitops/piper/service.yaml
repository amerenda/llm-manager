apiVersion: v1
kind: Service
metadata:
  name: piper-agent
  namespace: llm-manager
spec:
  type: ClusterIP
  selector:
    app: piper-agent
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
