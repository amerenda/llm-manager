apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piper-voices
  namespace: llm-manager
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
