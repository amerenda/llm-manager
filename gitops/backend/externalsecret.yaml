apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: agent-psk
  namespace: llm-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: agent-psk
    creationPolicy: Owner
  data:
    - secretKey: psk
      remoteRef:
        key: llm-manager-agent-psk
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: registration-secret
  namespace: llm-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: registration-secret
    creationPolicy: Owner
  data:
    - secretKey: secret
      remoteRef:
        key: llm-manager-registration-secret
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-oauth
  namespace: llm-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: github-oauth
    creationPolicy: Owner
  data:
    - secretKey: client-id
      remoteRef:
        key: llm-manager-github-client-id
        property: password
    - secretKey: client-secret
      remoteRef:
        key: llm-manager-github-client-secret
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: session-secret
  namespace: llm-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: session-secret
    creationPolicy: Owner
  data:
    - secretKey: secret
      remoteRef:
        key: llm-manager-session-secret
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: api-key-encryption-key
  namespace: llm-manager
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: api-key-encryption-key
    creationPolicy: Owner
  data:
    - secretKey: key
      remoteRef:
        key: llm-manager-api-key-encryption-key
        property: password
