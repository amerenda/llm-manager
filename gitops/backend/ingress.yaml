apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: llm-manager-backend-ingress
  namespace: llm-manager
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: llm-manager-backend.amer.dev
    external-dns.alpha.kubernetes.io/ttl: "300"
spec:
  ingressClassName: infra-traefik
  tls:
    - hosts:
        - llm-manager-backend.amer.dev
      secretName: llm-manager-backend-amer-dev-tls
  rules:
    - host: llm-manager-backend.amer.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: llm-manager-backend
                port:
                  number: 8081
