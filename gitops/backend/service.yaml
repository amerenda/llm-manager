apiVersion: v1
kind: Service
metadata:
  name: llm-manager-backend
  namespace: llm-manager
  labels:
    app: llm-manager-backend
spec:
  selector:
    app: llm-manager-backend
  ports:
    - name: http
      port: 8081
      targetPort: 8081
